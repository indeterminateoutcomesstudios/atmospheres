<p>Environments <a href {{action (action onCreate)}}>+</a></p>
<ul>
  {{#each environments as |environment|}}
    <li>
      <a href {{action (action onPlay environment)}}>{{environment.name}}</a>
      <a href {{action (action onDelete environment)}}>&times;</a>
    </li>
  {{/each}}
</ul>
